# FifteenShowreel 部署上线版本 1.0

发布日期：2026 年 8 月 14 日
版本：`1.0.0`

## 视觉与动效

- 新增约 4.5 秒的完整电影化 opening，包含幕布揭开、标题遮罩、位移、横向压缩与 Hero 接管。
- 为创作陈述、核心作品、工作维度、履历奖项和联系合作建立 GSAP + ScrollTrigger 分段进场。
- 英文大标题先进入，正文、卡片和媒体随后 stagger 出现；图片加入 reveal 与轻量 parallax。
- 修复 React StrictMode 下 opening 时间线可能被清理后无法退出的问题，并增加结束保险闸门。
- 完善 `prefers-reduced-motion` 降级路径，避免减少动态偏好的用户被长动画阻塞。

## 人物与 ASCII 效果

- 使用 Canvas2D 重建 21st.dev Custom ASCII Art 风格的多模式渲染管线。
- 当前人物采用 Braille 模式、灰度调色、胶片颗粒、轻微 glitch 与 flicker 动画。
- 人物照片已制作透明软轮廓，ASCII 与后期效果均受源图 alpha 约束，边缘向外渐隐，不再出现矩形卡片边界。
- 人物渲染仅在进入视口且页面可见时运行，并按桌面/移动设备限制帧率。

## 项目内容

- 新增《悔纪春归》：第二摄影助理，武汉传媒学院，短片电影。
- 新增《念念》：灯光助理，上海戏剧学院，短片电影。
- 新增《情绪封装器》：导演 / 编剧，湖南应用技术学院，微电影广告。
- 新增《逐光》：摄影，商业项目，微电影公益广告。
- 《远山不扰》岗位更新为“制片 / 数字影像工程师”。
- 《麦苗生长》展示图替换为指定海报，并转换为 WebP。
- 所有新增信息均提供中英双语，新增项目支持卡片点击、自动静帧海报和弹窗播放。

## 性能优化

- 删除发布目录中的未引用原片与重复大图，原文件保留在可逆归档目录。
- 所有网页交付视频均控制在 Cloudflare Pages `25 MiB` 单文件上限以内。
- 长视频改为连续分段播放，避免高码率原片直接进入站点。
- Hero 视频延迟到 opening handoff 后播放，降低首屏网络与解码竞争。
- 项目缩略图只加载 poster；Projects 模块继续使用懒加载分包。
- Canvas2D 动态背景和 ASCII 人像降低帧率，并处理页面隐藏、低性能设备和移动端降档。
- ScrollTrigger 元素仅在接近视口时启用 `will-change`，动画完成后立即清理。
- 项目轨道不再截获普通纵向滚轮，仅响应明确的横向主导手势。

## 可读性与一致性

- 提升全部项目卡片的英文标题、编号和 Current 标签字号。
- 统一项目标题、岗位、来源、类型及播放状态的数据结构。
- 从文字履历中移除已升级为完整播放项目的《悔纪春归》。
- 修复移动端 Hero 信息互相遮挡，并保持页面无横向溢出。
- 更新网站名称、页面描述、页脚日期和部署说明为正式 `1.0.0`。

## 视频分发准备

- 新增 R2 + HLS 播放能力：配置 `VITE_VIDEO_CDN_BASE_URL` 后，项目弹窗优先加载 R2 的 `master.m3u8`。
- Safari 使用原生 HLS，其他浏览器按需动态加载 `hls.js`，R2 未配置或 HLS 不可用时自动回退现有 MP4 分段。
- 新增 FFmpeg HLS 生成脚本和 Wrangler R2 上传脚本，支持 360p、480p、720p 自适应码率输出。
- 保持当前 Pages 部署可直接运行，不配置 R2 时不会改变现有播放逻辑。
